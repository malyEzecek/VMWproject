@model IEnumerable<CarsStore.Models.Transmission>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name_trans)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name_trans)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_trans }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_trans }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_trans })
        </td>
    </tr>
}

</table>
